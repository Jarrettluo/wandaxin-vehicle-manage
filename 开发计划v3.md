# 二手车商ERP系统需求文档（修订版）

## 1. 项目概述

### 1.1 项目名称
二手车商ERP管理系统

### 1.2 项目目标
为中小型二手车商提供全面的车辆管理、销售、财务统计和合伙人分配功能的SaaS平台，支持多租户架构。

### 1.3 核心功能模块
- 多租户管理系统
- 车辆库存管理
- 采购管理（支持合伙购买）
- **车辆整备管理**
- 销售管理
- **附加利润管理**
- 费用支出管理
- 利润统计与分配
- 报表分析
- 权限角色管理

## 2. 系统架构（保持不变）
（同原文档）

## 3. 详细功能需求（修订）

### 3.1 用户与权限管理（新增角色）

#### 3.1.1 角色定义（新增）
7. **整备专员**
    - 管理车辆整备任务
    - 记录整备项目和费用
    - 上传整备前后照片

#### 3.1.2 权限控制（不变）
（同原文档）

### 3.2 车辆管理模块（扩展）

#### 3.2.1 车辆信息字段（新增）
```
整备信息：
- 整备状态（待整备/整备中/已整备）
- 整备开始时间
- 整备完成时间
- 整备总费用
- 整备负责人

附加利润项：
- 可售附加物品列表
- 保险退费金额
- 其他附加收入项
```

### 3.3 采购管理模块（不变）
（同原文档）

### 3.4 新增：车辆整备管理模块

#### 3.4.1 整备流程设计
```
待整备 → 整备中 → 已整备
   ↓
整备任务分配 → 整备执行 → 费用记录 → 验收确认
```

#### 3.4.2 整备任务管理
1. **任务创建**
    - 自动生成（车辆入库后）
    - 手动创建（特殊情况）

2. **整备模板系统**
   ```json
   {
     "template_id": "T001",
     "template_name": "标准整备套餐",
     "tenant_id": 1,
     "items": [
       {
         "item_id": 1,
         "name": "全车精洗",
         "category": "清洁类",
         "estimated_cost": 200,
         "estimated_time": 2,
         "description": "包括内饰深度清洁、外观精洗"
       },
       {
         "item_id": 2,
         "name": "发动机舱清洗",
         "category": "清洁类",
         "estimated_cost": 150,
         "estimated_time": 1,
         "description": "发动机舱去油污、线路整理"
       }
     ],
     "total_estimated_cost": 350,
     "total_estimated_time": 3,
     "is_active": true
   }
   ```

3. **自定义整备项目**
    - 支持自由添加整备项目
    - 自定义项目分类
    - 预估成本和实际成本跟踪

#### 3.4.3 整备项目分类
```
1. 清洁类
   - 全车精洗
   - 内饰深度清洁
   - 发动机舱清洗
   - 空调系统清洗
   - 轮毂翻新

2. 美容类
   - 全车打蜡
   - 玻璃镀膜
   - 漆面划痕修复
   - 大灯翻新
   - 内饰皮革保养

3. 维修类
   - 更换轮胎
   - 更换刹车片
   - 更换机油三滤
   - 空调制冷剂加注
   - 电瓶更换

4. 检测类
   - 全车检测
   - 底盘检测
   - 电子系统检测
   - 安全性能检测

5. 自定义类
   - 用户自定义项目
```

#### 3.4.4 整备费用管理
- 材料成本记录
- 人工成本记录
- 外包费用记录
- 费用审批流程
- 实际成本 vs 预估成本分析

#### 3.4.5 整备验收流程
1. 整备前照片上传
2. 整备后照片上传
3. 整备报告生成
4. 验收人确认
5. 费用结算

### 3.5 新增：附加利润管理模块

#### 3.5.1 附加利润来源
```
1. 车辆附属物品销售
   - 行车记录仪
   - 车载导航
   - 倒车雷达
   - 车载冰箱
   - 座椅套等装饰品

2. 保险相关利润
   - 商业险退费
   - 交强险过户剩余
   - 延保服务销售

3. 金融服务利润
   - 贷款服务费
   - 保险代办费
   - 过户代办费

4. 其他增值服务
   - 车辆美容套餐
   - 延保服务
   - 保养套餐销售
```

#### 3.5.2 附加物品管理
```sql
CREATE TABLE vehicle_accessories (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    vehicle_id BIGINT NOT NULL,
    accessory_type VARCHAR(50), -- 物品类型
    brand VARCHAR(50),          -- 品牌
    model VARCHAR(50),          -- 型号
    purchase_price DECIMAL(10,2), -- 采购价（如果有）
    estimated_sale_price DECIMAL(10,2), -- 预估售价
    actual_sale_price DECIMAL(10,2), -- 实际售价
    status ENUM('installed', 'removed', 'sold', 'discarded'), -- 状态
    install_date DATE,
    remove_date DATE,
    sale_date DATE,
    description TEXT,
    images JSON, -- 物品照片
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (vehicle_id) REFERENCES vehicles(id)
);
```

#### 3.5.3 保险退费管理
```sql
CREATE TABLE insurance_refunds (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    vehicle_id BIGINT NOT NULL,
    insurance_type ENUM('commercial', 'compulsory'), -- 商业险/交强险
    original_premium DECIMAL(10,2), -- 原保费
    refund_amount DECIMAL(10,2),    -- 退费金额
    refund_date DATE,               -- 退费日期
    insurance_company VARCHAR(100), -- 保险公司
    policy_number VARCHAR(50),      -- 保单号
    effective_date DATE,            -- 生效日期
    expiry_date DATE,               -- 到期日期
    remaining_days INT,             -- 剩余天数
    refund_status ENUM('pending', 'applied', 'received', 'failed'),
    received_date DATE,             -- 到账日期
    notes TEXT,
    attachment VARCHAR(255),        -- 附件
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (vehicle_id) REFERENCES vehicles(id)
);
```

### 3.6 销售管理模块（扩展）

#### 3.6.1 销售流程扩展
```
原流程：
客户咨询 → 报价议价 → 销售合同 → 支出录入 → 完成销售

扩展流程：
客户咨询 → 报价议价 → 附加产品销售 → 金融服务推荐 → 
销售合同 → 费用计算 → 完成销售 → 利润分配
```

#### 3.6.2 销售支出类型扩展
- 原有支出类型...
- **整备相关支出**
    - 清洁美容费用
    - 维修配件费用
    - 检测费用
- **附加成本**
    - 附属物品采购成本
    - 保险退费手续费

### 3.7 财务管理模块（修订）

#### 3.7.1 利润计算公式（修订版）
```
// 收入部分
销售总收入 = 车辆售价 + 附加物品销售额 + 增值服务收入

// 成本部分
车辆基础成本 = 收购成本 + 整备总费用
附加成本 = 附属物品采购成本 + 保险退费手续费
总成本 = 车辆基础成本 + 附加成本 + 销售支出 + 其他运营成本

// 额外收入
额外收入 = 保险退费金额 + 其他退款收入

// 最终利润计算
毛利润 = 销售总收入 - 总成本
净利润 = 毛利润 + 额外收入 - 税费

// 合伙人分配（按投资比例分配净利润）
合伙人A应得 = (净利润 × 合伙人A比例) + 合伙人A投资本金
```

#### 3.7.2 财务报表扩展
**新增报表：**
1. **整备成本分析报表**
    - 单车辆整备明细
    - 整备项目成本分布
    - 整备效率分析（时间 vs 成本）

2. **附加利润分析报表**
    - 附加物品销售统计
    - 保险退费汇总
    - 增值服务收入分析

3. **综合利润分析报表**
    - 各利润组成部分占比
    - 单车综合利润率
    - 利润构成趋势分析

### 3.8 报表与分析模块扩展

#### 3.8.1 新增分析维度
1. **整备效率分析**
    - 平均整备成本
    - 整备周期分析
    - 整备质量评估

2. **附加价值分析**
    - 附属物品加价率
    - 保险退费回收率
    - 增值服务接受率

3. **综合盈利能力分析**
    - 单车总利润率
    - 各利润来源贡献度
    - 最优利润组合分析

#### 3.8.2 数据看板新增指标
```
实时监控指标：
- 今日整备完成数/进行中数
- 整备费用累计
- 附加物品销售金额
- 保险退费到账金额
- 综合利润率实时计算

预警提醒新增：
- 整备成本超预算预警
- 附加物品库存不足
- 保险退费逾期提醒
```

## 4. 非功能性需求（不变）
（同原文档）

## 5. 数据字典（扩展）

### 5.1 新增数据表设计

#### reconditioning_tasks（整备任务表）
```sql
CREATE TABLE reconditioning_tasks (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    vehicle_id BIGINT NOT NULL,
    task_number VARCHAR(50) UNIQUE, -- 任务编号
    status ENUM('pending', 'in_progress', 'completed', 'cancelled'),
    assigned_to BIGINT, -- 整备负责人
    estimated_start_date DATE,
    actual_start_date DATE,
    estimated_completion_date DATE,
    actual_completion_date DATE,
    estimated_total_cost DECIMAL(10,2),
    actual_total_cost DECIMAL(10,2),
    template_id BIGINT, -- 使用的模板ID
    notes TEXT,
    inspection_result TEXT, -- 验收结果
    inspected_by BIGINT, -- 验收人
    inspected_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (vehicle_id) REFERENCES vehicles(id),
    FOREIGN KEY (assigned_to) REFERENCES users(id),
    FOREIGN KEY (template_id) REFERENCES reconditioning_templates(id)
);
```

#### reconditioning_items（整备项目表）
```sql
CREATE TABLE reconditioning_items (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    task_id BIGINT NOT NULL,
    item_name VARCHAR(100) NOT NULL,
    category VARCHAR(50),
    estimated_cost DECIMAL(10,2),
    actual_cost DECIMAL(10,2),
    estimated_time_hours DECIMAL(4,1), -- 预估工时
    actual_time_hours DECIMAL(4,1),    -- 实际工时
    status ENUM('pending', 'in_progress', 'completed'),
    start_time TIMESTAMP,
    completion_time TIMESTAMP,
    material_cost DECIMAL(10,2),  -- 材料成本
    labor_cost DECIMAL(10,2),     -- 人工成本
    other_cost DECIMAL(10,2),     -- 其他成本
    description TEXT,
    before_images JSON,  -- 整备前照片
    after_images JSON,   -- 整备后照片
    notes TEXT,
    FOREIGN KEY (task_id) REFERENCES reconditioning_tasks(id)
);
```

#### reconditioning_templates（整备模板表）
```sql
CREATE TABLE reconditioning_templates (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    template_name VARCHAR(100) NOT NULL,
    description TEXT,
    is_active BOOLEAN DEFAULT true,
    items JSON NOT NULL, -- 存储模板项目配置
    created_by BIGINT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (tenant_id) REFERENCES tenants(id)
);
```

#### additional_profits（附加利润表）
```sql
CREATE TABLE additional_profits (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    vehicle_id BIGINT NOT NULL,
    profit_type ENUM('accessory_sale', 'insurance_refund', 'financial_service', 'other'),
    item_name VARCHAR(200),
    amount DECIMAL(10,2) NOT NULL,
    cost DECIMAL(10,2) DEFAULT 0.00, -- 成本（如果有）
    profit DECIMAL(10,2) GENERATED ALWAYS AS (amount - cost) STORED, -- 自动计算利润
    transaction_date DATE,
    status ENUM('pending', 'completed', 'cancelled'),
    description TEXT,
    attachment VARCHAR(255),
    created_by BIGINT,
    approved_by BIGINT,
    approved_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (vehicle_id) REFERENCES vehicles(id)
);
```

## 6. 接口设计（扩展）

### 6.1 新增API端点

#### 整备管理API
```
POST   /api/v1/reconditioning/tasks           # 创建整备任务
GET    /api/v1/reconditioning/tasks          # 获取整备任务列表
GET    /api/v1/reconditioning/tasks/{id}     # 获取任务详情
PUT    /api/v1/reconditioning/tasks/{id}     # 更新任务信息
POST   /api/v1/reconditioning/tasks/{id}/items  # 添加整备项目
PUT    /api/v1/reconditioning/tasks/{id}/complete  # 完成整备
POST   /api/v1/reconditioning/tasks/{id}/inspect  # 验收整备
GET    /api/v1/reconditioning/templates      # 获取整备模板
POST   /api/v1/reconditioning/templates      # 创建整备模板
```

#### 附加利润API
```
POST   /api/v1/vehicles/{id}/accessories     # 添加附属物品
GET    /api/v1/vehicles/{id}/accessories     # 获取车辆附属物品
POST   /api/v1/vehicles/{id}/accessories/{accId}/sell  # 销售附属物品
POST   /api/v1/vehicles/{id}/insurance-refunds  # 登记保险退费
PUT    /api/v1/insurance-refunds/{id}/status  # 更新退费状态
GET    /api/v1/additional-profits           # 获取附加利润列表
GET    /api/v1/additional-profits/summary   # 附加利润汇总
```

#### 综合利润计算API
```
GET    /api/v1/vehicles/{id}/profit-detailed  # 获取详细利润构成
POST   /api/v1/vehicles/{id}/profit-calculate  # 重新计算利润
GET    /api/v1/reports/comprehensive-profit  # 综合利润报表
```

## 7. 实施计划（调整）

### 7.1 第一阶段（1-2个月）
- 基础框架搭建
- 租户管理系统
- 车辆信息管理
- 基础权限控制

### 7.2 第二阶段（1-2个月）
- 采购管理功能
- 合伙人分配逻辑
- 整备管理基础功能
- 整备模板系统

### 7.3 第三阶段（1个月）
- 整备任务全流程
- 附加利润管理
- 保险退费管理

### 7.4 第四阶段（1个月）
- 综合利润计算
- 高级报表系统
- 数据看板完善

### 7.5 第五阶段（1个月）
- 系统优化
- 性能测试
- 用户验收测试
- 移动端适配

## 8. 业务规则与约束

### 8.1 整备业务规则
1. 车辆入库后自动生成待整备状态
2. 整备费用必须经过审批才计入成本
3. 整备验收不通过需重新整备
4. 整备超预算需要特殊审批

### 8.2 附加利润业务规则
1. 附属物品销售需记录采购成本（如已知）
2. 保险退费以实际到账为准
3. 附加利润分配按合伙人投资比例进行
4. 增值服务销售需要合同或协议

### 8.3 财务计算规则
1. 整备费用全部计入车辆成本
2. 附属物品单独核算成本和利润
3. 保险退费作为额外收入，不计入车辆成本
4. 综合利润 = 车辆销售利润 + 附加物品利润 + 其他附加收入

## 9. 测试用例要点

### 9.1 整备功能测试
- 整备任务创建和分配
- 整备费用录入和审批
- 整备验收流程
- 整备成本控制预警

### 9.2 附加利润测试
- 附属物品添加和销售
- 保险退费申请和跟踪
- 附加利润计算准确性
- 利润分配正确性

### 9.3 综合业务测试
- 完整车辆生命周期利润计算
- 多合伙人复杂分配场景
- 大数据量下的报表性能
- 系统间数据一致性

## 10. 部署与维护（不变）
（同原文档）

---

**文档版本控制**
| 版本 | 日期 | 修改内容 | 修改人 |
|------|------|----------|--------|
| 1.0  | 2024-01-15 | 初始版本创建 | 系统分析师 |
| 1.1  | 2024-01-20 | 补充财务报表需求 | 系统分析师 |
| **2.0** | **2024-01-25** | **新增整备管理和附加利润模块** | **系统分析师** |

**下一步开发建议**
1. 优先开发整备管理核心流程
2. 设计灵活的成本项配置系统
3. 实现附加利润的跟踪和管理
4. 完善综合利润计算引擎
5. 开发可视化的利润分析报表

本修订版需求文档已全面覆盖车辆整备和附加利润管理需求，建议开发团队按模块分阶段实施，确保核心业务逻辑的正确性和系统的扩展性。