# 二手车商ERP系统需求文档

## 1. 项目概述

### 1.1 项目名称
二手车商ERP管理系统

### 1.2 项目目标
为中小型二手车商提供全面的车辆管理、销售、财务统计和合伙人分配功能的SaaS平台，支持多租户架构。

### 1.3 核心功能模块
- 多租户管理系统
- 车辆库存管理
- 采购管理（支持合伙购买）
- 销售管理
- 费用支出管理
- 利润统计与分配
- 报表分析
- 权限角色管理

## 2. 系统架构

### 2.1 技术架构
```
前端：Vue.js/React + Ant Design
后端：Spring Boot/Node.js
数据库：PostgreSQL/MySQL
部署：Docker + Kubernetes
认证：JWT + OAuth2.0
```

### 2.2 多租户设计
- 每个公司独立数据库实例或Schema隔离
- 数据完全隔离，保证安全性
- 可配置的公司个性化设置

## 3. 详细功能需求

### 3.1 用户与权限管理

#### 3.1.1 角色定义
1. **系统管理员**
    - 管理所有租户
    - 系统配置维护
    - 数据备份与恢复

2. **公司管理员**
    - 管理本公司员工账号
    - 配置公司基础信息
    - 设置角色权限
    - 查看公司所有数据

3. **采购经理**
    - 添加/编辑车辆采购信息
    - 管理合伙人分配比例
    - 审核采购申请

4. **销售经理**
    - 管理销售流程
    - 添加销售支出
    - 确认车辆销售

5. **财务人员**
    - 查看财务报表
    - 管理支出分类
    - 利润分配计算

6. **普通员工**
    - 查看分配的数据
    - 提交采购/销售申请

#### 3.1.2 权限控制
- 基于角色的访问控制（RBAC）
- 功能级权限控制
- 数据级权限控制（如只能查看自己负责的车辆）

### 3.2 车辆管理模块

#### 3.2.1 车辆信息字段
```
基础信息：
- 车辆ID（唯一标识）
- VIN码
- 车牌号
- 品牌
- 车型
- 年款
- 排量
- 变速箱类型
- 燃油类型
- 车身颜色
- 座位数
- 里程数
- 生产日期
- 登记日期

车况信息：
- 事故记录（有/无）
- 事故描述
- 维修记录
- 过户次数
- 车辆等级（A/B/C/D）

采购信息：
- 收购价
- 收购日期
- 前车主信息
- 收购方式（全款/按揭）
- 合伙人分配比例（JSON格式）

媒体信息：
- 车辆照片（至少10张）
- 检测报告
- 视频资料

状态信息：
- 当前状态（待入库/在库/已售/维修中）
- 入库时间
- 库位编号
- 备注
```

#### 3.2.2 车辆状态流转
```
待入库 → 在库 → 已售
          ↓
        维修中 → 在库
```

### 3.3 采购管理模块

#### 3.3.1 采购流程
1. 采购申请提交
2. 采购经理审核
3. 设置合伙人分配比例
4. 支付记录
5. 车辆入库

#### 3.3.2 合伙人分配
```
数据结构示例：
{
  "vehicle_id": "001",
  "total_cost": 100000,
  "partners": [
    {
      "user_id": 1,
      "name": "张三",
      "investment": 60000,
      "percentage": 60,
      "is_manager": true
    },
    {
      "user_id": 2,
      "name": "李四",
      "investment": 40000,
      "percentage": 40,
      "is_manager": false
    }
  ]
}
```

### 3.4 销售管理模块

#### 3.4.1 销售流程
1. 客户咨询登记
2. 报价与议价
3. 销售合同生成
4. 支出费用录入
5. 完成销售
6. 利润计算

#### 3.4.2 销售支出类型
- 整备费用
- 过户费用
- 广告宣传费
- 佣金提成
- 物流运输费
- 其他费用（可自定义）

### 3.5 财务管理模块

#### 3.5.1 利润计算公式
```
销售总收入 = 车辆售价
总支出 = 收购成本 + 所有销售支出
毛利润 = 销售总收入 - 总支出
净利润 = 毛利润 - 税费 - 其他扣减

合伙人分配：
合伙人A应得 = (净利润 × 合伙人A比例) + 合伙人A投资本金
```

#### 3.5.2 财务报表
- 单车辆利润明细表
- 月度利润汇总表
- 合伙人分配明细表
- 支出分类统计表
- 现金流报表

### 3.6 报表与分析模块

#### 3.6.1 核心报表
1. **库存分析报表**
    - 在库车辆数量
    - 库龄分析
    - 品牌分布
    - 价格区间分布

2. **销售分析报表**
    - 月度销售趋势
    - 热销车型排行
    - 销售周期分析
    - 利润率分析

3. **合伙人报表**
    - 投资回报率
    - 分红明细
    - 历史合作记录

#### 3.6.2 数据看板
- 关键指标实时展示
- 可视化图表
- 预警提醒（库存超期、利润过低等）

## 4. 非功能性需求

### 4.1 性能要求
- 页面响应时间 < 3秒
- 支持100个并发用户
- 数据导出速度：10000条记录 < 30秒

### 4.2 安全性要求
- 数据传输加密（HTTPS）
- 敏感数据加密存储
- 防止SQL注入、XSS攻击
- 操作日志记录
- 定期数据备份

### 4.3 可用性要求
- 系统可用性 > 99.5%
- 移动端适配
- 友好的用户界面
- 操作引导和提示

### 4.4 扩展性要求
- 支持插件式功能扩展
- 易于添加新的支出类型
- 支持API接口扩展

## 5. 数据字典

### 5.1 核心数据表设计

#### tenants（租户表）
```sql
CREATE TABLE tenants (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    company_name VARCHAR(100) NOT NULL,
    contact_person VARCHAR(50),
    phone VARCHAR(20),
    email VARCHAR(100),
    address TEXT,
    status ENUM('active', 'inactive') DEFAULT 'active',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

#### vehicles（车辆表）
```sql
CREATE TABLE vehicles (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    vin VARCHAR(50) UNIQUE,
    license_plate VARCHAR(20),
    brand VARCHAR(50),
    model VARCHAR(50),
    year INT,
    purchase_price DECIMAL(12,2),
    sale_price DECIMAL(12,2),
    current_status ENUM('pending', 'in_stock', 'sold', 'maintenance'),
    stock_in_date DATE,
    stock_location VARCHAR(50),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (tenant_id) REFERENCES tenants(id)
);
```

#### partners（合伙人表）
```sql
CREATE TABLE vehicle_partners (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    vehicle_id BIGINT NOT NULL,
    user_id BIGINT NOT NULL,
    investment_amount DECIMAL(12,2),
    profit_percentage DECIMAL(5,2),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (vehicle_id) REFERENCES vehicles(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

## 6. 接口设计

### 6.1 RESTful API设计原则
- 使用JSON格式
- 统一响应格式
- 合理的HTTP状态码
- API版本控制

### 6.2 主要API端点

#### 车辆管理
```
GET    /api/v1/vehicles          # 获取车辆列表
POST   /api/v1/vehicles          # 添加车辆
GET    /api/v1/vehicles/{id}     # 获取车辆详情
PUT    /api/v1/vehicles/{id}     # 更新车辆信息
DELETE /api/v1/vehicles/{id}     # 删除车辆
POST   /api/v1/vehicles/import   # 批量导入
GET    /api/v1/vehicles/export   # 导出数据
```

#### 销售管理
```
POST   /api/v1/sales              # 创建销售记录
POST   /api/v1/sales/{id}/expenses # 添加销售支出
POST   /api/v1/sales/{id}/complete # 完成销售
GET    /api/v1/sales/profit/{id}  # 计算单车辆利润
```

## 7. 实施计划

### 7.1 第一阶段（1-2个月）
- 基础框架搭建
- 租户管理系统
- 车辆信息管理
- 基础权限控制

### 7.2 第二阶段（1-2个月）
- 采购管理功能
- 合伙人分配逻辑
- 销售流程管理

### 7.3 第三阶段（1个月）
- 财务计算模块
- 报表系统
- 数据看板

### 7.4 第四阶段（1个月）
- 系统优化
- 性能测试
- 用户验收测试

## 8. 部署与维护

### 8.1 部署环境要求
- 服务器：至少4核8G内存
- 数据库：PostgreSQL 12+
- 缓存：Redis
- 文件存储：对象存储或本地存储

### 8.2 监控与维护
- 系统健康检查
- 错误日志收集
- 性能监控
- 定期数据备份

## 9. 附录

### 9.1 术语解释
- **租户**：指使用系统的不同二手车公司
- **合伙人**：共同投资购买车辆的投资者
- **库龄**：车辆在库时间长度
- **整备费用**：车辆售前整理和维修费用

### 9.2 假设与约束
- 假设用户具备基础的计算机操作能力
- 系统初期支持最多100个租户
- 需要稳定的网络连接
- 移动端优先支持H5，后续考虑小程序

---

**文档版本控制**
| 版本 | 日期 | 修改内容 | 修改人 |
|------|------|----------|--------|
| 1.0  | 2024-01-15 | 初始版本创建 | 系统分析师 |
| 1.1  | 2024-01-20 | 补充财务报表需求 | 系统分析师 |

**下一步建议**
1. 根据本文档进行技术方案设计
2. 创建详细的数据模型设计
3. 制定开发规范和代码结构
4. 搭建开发环境
5. 开始迭代开发，每2周进行一次演示和调整

本需求文档可作为开发依据，建议在开发过程中保持与业务方的持续沟通，确保系统符合实际业务需求。